{
	    "scripts": {
	        "preinstall": "npm install -g lerna@2.2.0",
	        "heroku-postbuild": "cp -R packages/ui/build/* packages/api/client",
	        "start": "lerna run start",
	        "start:heroku": "cd packages/api && npm run start:heroku",
	        "postinstall": "lerna bootstrap && lerna run build && cd packages/api && npm run generate-swagger-lib && grunt swagger"
	    },
	    "devDependencies": {
	        "lerna": "2.2.0"
	    }
} 