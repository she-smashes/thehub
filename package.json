{
	"scripts": {
		"preinstall": "npm install -g lerna@2.2.0 loopback-cli@latest && lerna bootstrap",
		"heroku-postbuild": "lerna run build && cp -R packages/ui/build/* packages/api/client",
		"start": "lerna run start",
		"start:heroku": "cd packages/api && npm run start:heroku",
		"postinstall": "cd packages/api && npm run generate-swagger-lib && grunt swagger"
	},
	"devDependencies": {
		"lerna": "2.2.0"
	}
}